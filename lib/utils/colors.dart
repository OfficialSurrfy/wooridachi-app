import 'package:flutter/material.dart';

class AppColor {
  static const Color signatureColor = Color.fromRGBO(67, 202, 255, 1);

// Add more colors as needed
}
const Color signatureColor = Color.fromRGBO(67, 202, 255, 1);
const mobileBackgroundColor = Colors.grey;
const primaryColor = Color(0xFF36aff3);
const secondaryColor = Colors.black;

const selectedColor = Color(0xFF36aff3);
const unselectedColor = Colors.grey;
